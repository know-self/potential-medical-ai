import React from 'react';
import {Clock3,ShieldCheck,Sparkles} from 'lucide-react';
const prompts=['Summarize recent guidelines for heart failure','Evidence for SGLT2i in CKD without diabetes','Explain common causes of persistent headache'];
export default function WelcomeMessage({onExampleClick}){return <section className="welcome-state"><div className="welcome-icon"><Sparkles size={28}/></div><h1>Your medical assistant, grounded in trusted evidence.</h1><p>Ask health questions, attach source documents, and keep user-confirmed context in one secure conversation.</p><div className="prompt-grid">{prompts.map(x=><button key={x} onClick={()=>onExampleClick(x)}>{x}</button>)}</div><div className="trust-row"><div><ShieldCheck size={19}/><span><strong>Grounded evidence</strong><small>Versioned citations and review state</small></span></div><div><ShieldCheck size={19}/><span><strong>Server-side safety</strong><small>No browser clinical fallback</small></span></div><div><Clock3 size={19}/><span><strong>Freshness policy</strong><small>Required sources fail closed</small></span></div></div></section>}
