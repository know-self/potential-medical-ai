import React from 'react';
import { Activity, BookOpen, Clock3, MessageCircle, Plus, Settings, ShieldCheck, Stethoscope, Trash2, Users } from 'lucide-react';
import ChatNameEditor from './ChatNameEditor';
const nav=[[MessageCircle,'Chat'],[Users,'Patients'],[BookOpen,'Evidence'],[Activity,'Tools'],[Settings,'Settings']];
export default function ChatSidebar({chats,currentChatId,onSelectChat,onNewChat,onDeleteChat,onUpdateChatTitle,isOpen,onClose}){return <aside className={`navigation-rail ${isOpen?'open':''}`}><div className="rail-inner"><button className="new-chat-button" onClick={onNewChat}><Plus size={17}/> New chat <kbd>⌘ K</kbd></button><nav className="primary-nav">{nav.map(([Icon,label],i)=><button className={i===0?'active':''} key={label}><Icon size={17}/><span>{label}</span></button>)}</nav><div className="rail-divider"/><div className="section-label"><Clock3 size={15}/> Recent chats</div><div className="chat-list">{chats.length===0?<div className="empty-mini">No conversations yet.</div>:chats.map(chat=><div key={chat.id} className={`chat-row ${currentChatId===chat.id?'active':''}`} onClick={()=>{onSelectChat(chat.id);onClose?.();}}><MessageCircle size={14}/><div><ChatNameEditor title={chat.title} onSave={title=>onUpdateChatTitle(chat.id,title)}/><small>{chat.messageCount||0} messages</small></div><button onClick={e=>{e.stopPropagation();onDeleteChat(chat.id);}} aria-label="Delete"><Trash2 size={13}/></button></div>)}</div><div className="rail-status"><ShieldCheck size={19}/><div><strong>System status</strong><span>All guarded services ready</span></div><i/></div><div className="rail-footer"><Stethoscope size={17}/><div><strong>Potential Medical AI</strong><span>Conversational platform</span></div></div></div></aside>}
