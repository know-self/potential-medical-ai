# Terminal CLI validation scope

Validation is enforced by `tests/terminalCli.test.js` and the repository CI workflow. The tests cover command dispatch, one-shot prompts, interactive command parsing, SSE streaming, authorization headers, locale and attachment forwarding.
